// New questions added in the React version.
// Same card shape as the original deck: { cat, q, a, fq, fa }.
// Headlined by the new Next.js stack, plus deeper coverage of every existing topic.
// Answers use the same mini-markdown: **bold**, `code`, "- " bullet lists, and ```fenced``` code.

export const newCards = [
  /* ============================ Next.js ============================ */
  {
    cat: "Next.js",
    q: "What's the difference between the App Router and the Pages Router?",
    a: "- **Pages Router** (`pages/`): file-based routes, `getStaticProps`/`getServerSideProps` for data, every route is a client/SSR React component.\n- **App Router** (`app/`): built on **React Server Components**, uses nested `layout.js`/`page.js`, `async` components for data, streaming, and Server Actions.\n\nApp Router is the default for new apps since Next 13.4.",
    fq: "Can both routers coexist in one app?",
    fa: "Yes — `app/` and `pages/` can live side by side during migration. If the same path exists in both, `app/` wins. You migrate incrementally.",
  },
  {
    cat: "Next.js",
    q: "In the App Router, are components server or client by default?",
    a: "**Server Components by default.** They render on the server, can be `async`, can read the filesystem/DB directly, and ship **zero JS** to the client. You opt into a Client Component with the `\"use client\"` directive at the top of the file.",
    fq: "What forces you to add `\"use client\"`?",
    fa: "Anything interactive or browser-only: hooks (`useState`/`useEffect`), event handlers (`onClick`), or Web APIs (`window`, `localStorage`). Those only work in Client Components.",
  },
  {
    cat: "Next.js",
    q: "What is a React Server Component (RSC)?",
    a: "A component that runs **only on the server** and never ships its code to the browser. It can fetch data directly (`await db.query(...)`), keep secrets server-side, and shrink the client bundle. Its rendered output is serialized and streamed to the client.",
    fq: "Can a Server Component render a Client Component (and vice versa)?",
    fa: "Server can render Client, passing **serializable** props. The reverse is limited: a Client Component can't import a Server Component, but it can receive one as `children`/props.",
  },
  {
    cat: "Next.js",
    q: "How do you fetch data in an App Router Server Component?",
    a: "Make the component `async` and `await` directly:\n\n```jsx\nexport default async function Page() {\n  const res = await fetch('https://api/...');\n  const data = await res.json();\n  return <List items={data} />;\n}\n```\n\nNo `getServerSideProps` needed.",
    fq: "Does `fetch` cache the result?",
    fa: "It depends on the version. Next 14 cached `fetch` by default (`force-cache`). **Next 15 does not cache by default** — you opt in with `cache: 'force-cache'` or `next: { revalidate }`. Always check the version.",
  },
  {
    cat: "Next.js",
    q: "Explain SSR, SSG, and ISR in Next.js.",
    a: "- **SSG (static)**: HTML built at **build time**, served from CDN — fastest, for content that rarely changes.\n- **SSR (server-side)**: HTML built **per request** — for personalized/always-fresh data.\n- **ISR (Incremental Static Regeneration)**: static pages that **re-generate in the background** after a `revalidate` window — static speed with fresh-ish data.",
    fq: "How do you enable ISR in the App Router?",
    fa: "Set `export const revalidate = 60` on the route (seconds), or per-fetch with `next: { revalidate: 60 }`. On-demand: call `revalidatePath()` / `revalidateTag()` after a mutation.",
  },
  {
    cat: "Next.js",
    q: "`getStaticProps` vs `getServerSideProps` (Pages Router)?",
    a: "- `getStaticProps`: runs at **build time** → a static page (SSG); add `revalidate` for ISR.\n- `getServerSideProps`: runs on **every request** on the server (SSR).\n\nBoth run only on the server and are stripped from the browser bundle.",
    fq: "What replaces them in the App Router?",
    fa: "Nothing directly — you fetch inside `async` Server Components. The caching config (`fetch` options, `revalidate`, dynamic APIs) decides static vs dynamic behavior.",
  },
  {
    cat: "Next.js",
    q: "What does `next/link` do that a plain `<a>` doesn't?",
    a: "`<Link>` gives **client-side navigation** (no full page reload) and **prefetches** the linked route's code/data when the link scrolls into view (in production), so navigation feels instant.",
    fq: "How do you disable prefetching?",
    fa: "`<Link prefetch={false}>`. Useful for rarely-clicked links to save bandwidth.",
  },
  {
    cat: "Next.js",
    q: "What is `next/image` and why use it?",
    a: "An `<Image>` component with automatic **resizing/optimization**, modern formats (WebP/AVIF), **lazy loading** by default, and reserved space to prevent layout shift.",
    fq: "Why does it require `width`/`height` (or `fill`)?",
    fa: "To reserve the correct aspect-ratio box before the image loads, avoiding **Cumulative Layout Shift (CLS)**. Use `fill` with a positioned parent when dimensions are unknown.",
  },
  {
    cat: "Next.js",
    q: "What are Route Handlers?",
    a: "API endpoints in the App Router — export HTTP-method functions from `app/**/route.js`:\n\n```js\nexport async function GET(req) {\n  return Response.json({ ok: true });\n}\n```\n\nThey replace `pages/api/*`.",
    fq: "Are GET Route Handlers cached?",
    fa: "In Next 15, **no** — they're dynamic by default. (Next 14 cached them by default.) Opt into caching with segment config like `export const dynamic = 'force-static'`.",
  },
  {
    cat: "Next.js",
    q: "What is Next.js Middleware?",
    a: "Code in `middleware.ts` at the project root that runs **before a request completes**, on the Edge runtime. Used for auth gating, redirects, rewrites, A/B tests, and headers/cookies. A `matcher` config controls which paths it runs on.",
    fq: "Why shouldn't you query your database in middleware?",
    fa: "It runs on the **Edge runtime** (limited APIs, must be fast) for every matched request. Do lightweight work (read a token) and defer heavy queries to the route.",
  },
  {
    cat: "Next.js",
    q: "What are Server Actions?",
    a: "Async functions marked `\"use server\"` that run on the server and can be called from forms/components to **mutate data** without a separate API route:\n\n```jsx\nasync function add(formData) {\n  \"use server\";\n  await db.insert(...);\n  revalidatePath('/list');\n}\n// <form action={add}>\n```",
    fq: "How do you refresh the UI after a Server Action mutates data?",
    fa: "Call `revalidatePath()`/`revalidateTag()` inside the action (or `redirect()`); the affected Server Components re-render with fresh data.",
  },
  {
    cat: "Next.js",
    q: "What's the role of `layout.js`?",
    a: "It defines shared UI that **wraps** a route segment and its children. Layouts **persist across navigation** — they don't re-render when you move between sibling pages, preserving state (scroll, inputs). The root layout must render `<html>` and `<body>`.",
    fq: "How does `layout.js` differ from `template.js`?",
    fa: "A `layout` persists and keeps state between navigations. A `template` creates a **new instance** each navigation (state resets, effects re-run) — good for enter animations or per-page resets.",
  },
  {
    cat: "Next.js",
    q: "What do `loading.js` and `error.js` do?",
    a: "- `loading.js`: an instant loading UI shown via a **Suspense boundary** while the segment's data loads (enables streaming).\n- `error.js`: an **error boundary** for the segment — catches render errors and shows a fallback with a `reset()` function.",
    fq: "Why must `error.js` be a Client Component?",
    fa: "Error boundaries rely on React state/lifecycle to catch and recover, which only works client-side — so `error.js` needs `\"use client\"`.",
  },
  {
    cat: "Next.js",
    q: "How do dynamic routes work in the App Router?",
    a: "Bracketed folder names:\n- `[id]` → one dynamic segment (`/post/123`).\n- `[...slug]` → catch-all (`/a/b/c`).\n- `[[...slug]]` → optional catch-all (also matches the base path).\n\nThe values arrive in the `params` prop.",
    fq: "How do you pre-render dynamic pages at build time?",
    fa: "Export `generateStaticParams()` returning the list of params; Next statically generates one page per entry — the App Router equivalent of `getStaticPaths`.",
  },
  {
    cat: "Next.js",
    q: "What is streaming with Suspense in the App Router?",
    a: "Instead of waiting for the whole page, Next **streams** HTML in chunks. Wrap slow parts in `<Suspense fallback={...}>` (or use `loading.js`) so fast content shows immediately while slow data streams in behind it.",
    fq: "Which real metric does streaming improve?",
    fa: "**TTFB / First Contentful Paint** — users see the shell and fast content sooner instead of staring at a blank page until the slowest query resolves.",
  },
  {
    cat: "Next.js",
    q: "How does the Metadata API work?",
    a: "Export metadata from a `layout`/`page`:\n\n```js\nexport const metadata = { title: 'Home', description: '...' };\n```\n\nFor dynamic values, export an async `generateMetadata({ params })`. Next injects the `<head>` tags — there's no `next/head` in the App Router.",
    fq: "How do you set a title template across pages?",
    fa: "In the root layout: `title: { template: '%s | MyApp', default: 'MyApp' }`. Child pages set just their part and it fills `%s`.",
  },
  {
    cat: "Next.js",
    q: "How do environment variables work in Next.js?",
    a: "Vars in `.env*` are **server-only by default**. To expose one to the browser, prefix it with `NEXT_PUBLIC_` — those are inlined into the client bundle at build time.",
    fq: "Is it safe to put an API secret in a `NEXT_PUBLIC_` variable?",
    fa: "No — `NEXT_PUBLIC_` values are baked into client JS and visible to everyone. Keep secrets unprefixed (server-only) and use them in Server Components/Actions/Route Handlers.",
  },
  {
    cat: "Next.js",
    q: "What causes a hydration error in Next.js?",
    a: "The server-rendered HTML doesn't match what React produces on the client. Common causes: using `window`/`Date.now()`/`Math.random()` during render, invalid HTML nesting, or a browser extension mutating the DOM.",
    fq: "How do you render something only on the client to avoid a mismatch?",
    fa: "Gate it behind an effect: set a `mounted` flag in `useEffect` and render the dynamic bit only when `mounted`, or use `next/dynamic` with `{ ssr: false }`.",
  },
  {
    cat: "Next.js",
    q: "What's the difference between `redirect()` and `notFound()`?",
    a: "Server helpers from `next/navigation`:\n- `redirect('/path')` — stop rendering and send the user elsewhere.\n- `notFound()` — stop rendering and show the nearest `not-found.js` (404).\n\nBoth throw internally, so code after them doesn't run.",
    fq: "Can you call `redirect()` inside a Server Action or Route Handler?",
    fa: "Yes — it's common after a mutation to send the user to the result page. It works in Server Components, Server Actions, and Route Handlers.",
  },
  {
    cat: "Next.js",
    q: "What are parallel and intercepting routes?",
    a: "- **Parallel routes** (`@slot`): render multiple independent pages in one layout at once (e.g. dashboard panels), each with its own loading/error.\n- **Intercepting routes** (`(.)`, `(..)`): show a route's content in the current context — the classic use is opening an item in a **modal** while keeping a shareable URL.",
    fq: "Give a real use case for intercepting routes.",
    fa: "A modal with its own URL: clicking a thumbnail opens `/photo/1` as a modal over the feed, but visiting `/photo/1` directly (or refreshing) shows the full page.",
  },
  {
    cat: "Next.js",
    q: "Where should data fetching live — Server or Client Components?",
    a: "Prefer **Server Components** (closer to the data, no client waterfall, secrets stay server-side). Use Client Components for interactivity; if they need data, pass it down as props or fetch via Route Handlers.",
    fq: "How do you pass server-fetched data into a Client Component?",
    fa: "Fetch in the parent Server Component and pass it as **serializable props**. The Client Component receives it already-resolved — no client fetch needed.",
  },
  {
    cat: "Next.js",
    q: "Explain tag-based caching: `revalidateTag` / `revalidatePath`.",
    a: "Tag your fetches: `fetch(url, { next: { tags: ['posts'] } })`. After a write, call `revalidateTag('posts')` (or `revalidatePath('/blog')`) to purge just that data, so the next request re-fetches while everything else stays cached.",
    fq: "Why prefer tag invalidation over time-based `revalidate`?",
    fa: "Tags invalidate **exactly** what changed the moment it changes (event-driven), instead of serving stale data until a timer expires. Best when you control the writes.",
  },
  {
    cat: "Next.js",
    q: "How do you do client-side data fetching/mutations in Next.js?",
    a: "For client interactivity, use **SWR** or **React Query** against Route Handlers, or call **Server Actions**. These handle caching, dedup, revalidation, loading, and error states for you.",
    fq: "Why is raw `useEffect` + `fetch` discouraged for data?",
    fa: "It runs after render (waterfalls), has no caching/dedup, and makes you hand-roll loading/error/refetch. SWR / React Query / Server Components solve those out of the box.",
  },
  {
    cat: "Next.js",
    q: "How does `useRouter` differ between App and Pages routers?",
    a: "Different import and API. App Router: `import { useRouter } from 'next/navigation'` (`push`, `replace`, `refresh`, `back`; no `query`/`pathname`). Pages Router: `import { useRouter } from 'next/router'` (`query`, `pathname`, `asPath`).",
    fq: "How do you read the pathname / search params in the App Router?",
    fa: "Use the dedicated Client hooks `usePathname()` and `useSearchParams()` from `next/navigation`.",
  },

  /* ============================ React ============================ */
  {
    cat: "React",
    q: "When should you use `useMemo` vs `useCallback`?",
    a: "- `useMemo` memoizes a **computed value** so an expensive calculation doesn't re-run every render.\n- `useCallback` memoizes a **function reference** so it stays stable across renders.\n\nBoth exist to avoid wasted work/re-renders — not to sprinkle everywhere.",
    fq: "When is `useCallback` actually necessary?",
    fa: "When the function is passed to a `memo`-ized child (stable identity prevents its re-render) or used as another hook's dependency. Otherwise it usually adds overhead for no gain.",
  },
  {
    cat: "React",
    q: "What does `React.memo` do?",
    a: "Wraps a component so it **skips re-rendering** when its props are shallow-equal to last time — memoizing output by props.",
    fq: "Why might a `memo` component still re-render every time?",
    fa: "If a parent passes a new object/array/function each render (new reference), the shallow compare fails. Stabilize those props with `useMemo`/`useCallback`.",
  },
  {
    cat: "React",
    q: "`useEffect` vs `useLayoutEffect`?",
    a: "Both run after render, but `useLayoutEffect` fires **synchronously after DOM mutation, before paint** — use it to measure layout or mutate the DOM without flicker. `useEffect` runs **after paint** (async) and is the default.",
    fq: "What's the downside of `useLayoutEffect`?",
    fa: "It blocks painting, so heavy work there hurts perceived performance, and it warns during SSR. Use it only when you must read/write layout before paint.",
  },
  {
    cat: "React",
    q: "What is `useReducer` and when beats `useState`?",
    a: "A hook managing state via a `(state, action) => newState` reducer. Prefer it when state is **complex/interdependent**, when the next state depends on the previous, or when you want centralized, testable update logic.",
    fq: "How does `useReducer` help performance?",
    fa: "`dispatch` has a **stable identity**, so you can pass it down without breaking `memo`/`useCallback` — avoiding the churn of threading many setter callbacks.",
  },
  {
    cat: "React",
    q: "Controlled vs uncontrolled components?",
    a: "- **Controlled**: React state is the source of truth (`value` + `onChange`).\n- **Uncontrolled**: the DOM holds the value; you read it via a `ref` (or `defaultValue`).\n\nControlled enables per-keystroke validation/formatting; uncontrolled is simpler and closer to native.",
    fq: "What triggers the 'you provided a `value` without `onChange`' warning?",
    fa: "Setting `value` with no `onChange` makes a read-only input. Fix by adding `onChange` (controlled) or using `defaultValue` (uncontrolled).",
  },
  {
    cat: "React",
    q: "What is `useRef` for?",
    a: "A mutable container (`ref.current`) that **persists across renders without triggering one**. Uses: DOM node access, storing timers/previous values, or any mutable instance value that shouldn't drive the UI.",
    fq: "Why not just use a variable or state instead?",
    fa: "A local variable resets each render; state re-renders on change. A ref persists and is silent — ideal for values that shouldn't cause re-renders.",
  },
  {
    cat: "React",
    q: "What is a React Portal?",
    a: "`createPortal(children, domNode)` renders children into a **different DOM node** (e.g. `document.body`) while keeping them in the React tree. Great for modals, tooltips, and toasts that must escape parent `overflow`/`z-index`.",
    fq: "Do events bubble through a portal to the React parent?",
    fa: "Yes — bubbling follows the **React tree**, not the DOM tree. A click inside a portal still reaches its React parent's handlers.",
  },
  {
    cat: "React",
    q: "What is an Error Boundary?",
    a: "A class component with `getDerivedStateFromError`/`componentDidCatch` that **catches render-time errors** in its subtree and shows a fallback instead of crashing the whole app.",
    fq: "What errors do boundaries NOT catch?",
    fa: "Errors in event handlers, async code (`setTimeout`, promises), during SSR, and errors thrown in the boundary itself. Handle those with `try/catch`.",
  },
  {
    cat: "React",
    q: "Why does React need `key` props in lists?",
    a: "Keys let React match elements across renders to decide what to reuse, move, or discard. Without stable keys it falls back to index matching and can **attach the wrong state to the wrong item**.",
    fq: "Why is the array index a risky key?",
    fa: "If the list reorders/inserts/deletes, indexes shift, so React keeps old state on the wrong row — wrong inputs, checkboxes, animations. Use a stable unique id.",
  },
  {
    cat: "React",
    q: "What does StrictMode do in development?",
    a: "A dev-only wrapper that surfaces bugs: it **double-invokes** render, effects (mount→unmount→mount), and state updaters to expose impure renders and missing cleanup. It renders nothing and does nothing in production.",
    fq: "My effect runs twice on mount — bug?",
    fa: "That's StrictMode testing your cleanup. Make effects idempotent and clean up (abort fetches, clear subscriptions); the double-run is gone in production.",
  },

  /* ============================ JavaScript ============================ */
  {
    cat: "JavaScript",
    q: "Explain the event loop: microtasks vs macrotasks.",
    a: "The call stack runs sync code. When empty, the loop drains **all microtasks** (Promise callbacks, `queueMicrotask`) first, then takes **one macrotask** (`setTimeout`, I/O, events), then drains microtasks again, and repeats.",
    fq: "What logs first: `setTimeout(fn, 0)` or a resolved `Promise.then`?",
    fa: "The Promise. Microtasks run before the next macrotask (`setTimeout`) even with a 0 ms delay.",
  },
  {
    cat: "JavaScript",
    q: "How does `this` binding work?",
    a: "`this` is set by **how a function is called**:\n- `obj.fn()` → `obj`.\n- `fn()` → `undefined` (strict) or global.\n- `call`/`apply`/`bind` → explicit.\n- `new` → the new instance.\n- Arrow functions → inherit `this` lexically (no own `this`).",
    fq: "Why do arrow functions fix the classic callback `this` bug?",
    fa: "They don't bind their own `this`; they capture it from the enclosing scope, so `this` inside a callback still points at the surrounding object/component.",
  },
  {
    cat: "JavaScript",
    q: "Deep copy vs shallow copy?",
    a: "- **Shallow** (`{...obj}`, `Object.assign`): copies the top level; nested objects stay shared references.\n- **Deep**: nested objects are cloned too. Modern deep clone: `structuredClone(obj)`.",
    fq: "Pitfalls of `JSON.parse(JSON.stringify(obj))`?",
    fa: "It drops `undefined`, functions, and symbols, mangles `Date`/`Map`/`Set` (Dates become strings), and throws on circular refs. `structuredClone` handles most of these.",
  },
  {
    cat: "JavaScript",
    q: "What is a closure, and a common gotcha?",
    a: "A function that **remembers variables from its lexical scope** even after that scope returned. Gotcha: capturing a loop variable declared with `var` — all the closures share one binding.",
    fq: "How does `let` fix the loop-closure bug?",
    fa: "`let` creates a **fresh binding each iteration**, so every closure captures its own value. `var` has one function-scoped binding shared by all.",
  },
  {
    cat: "JavaScript",
    q: "`Map` vs plain object for key-value data?",
    a: "- `Map`: any key type (objects, functions), preserves insertion order, has `.size`, easy iteration, no prototype collisions.\n- `Object`: string/symbol keys, JSON-friendly, best for fixed-shape records.\n\nReach for `Map` on frequent add/remove or non-string keys.",
    fq: "Name a subtle bug from using a plain object as a hash map.",
    fa: "Inherited keys: `'toString'`/`'constructor'` collide with prototype members and `'hasOwnProperty' in obj` is always true. `Map` (or `Object.create(null)`) avoids it.",
  },
  {
    cat: "JavaScript",
    q: "Debounce vs throttle?",
    a: "- **Debounce**: wait until activity **stops** for N ms, then fire once (search-as-you-type, resize end).\n- **Throttle**: fire at most **once per N ms** during continuous activity (scroll, mousemove).\n\nDebounce = 'after the storm'; throttle = 'rate limit'.",
    fq: "Which fits a search-input autocomplete?",
    fa: "Debounce — hit the API only after the user pauses typing, not on every keystroke.",
  },
  {
    cat: "JavaScript",
    q: "`==` vs `===`?",
    a: "`===` is strict equality (no coercion). `==` coerces types first, which surprises (`0 == ''`, `[] == false`, `null == undefined` are all true). Prefer `===`, except the idiomatic `x == null`.",
    fq: "What does `x == null` match?",
    fa: "Exactly `null` and `undefined` — nothing else. A common shorthand nullish check.",
  },
  {
    cat: "JavaScript",
    q: "What are `async`/`await` really doing?",
    a: "`async` makes a function return a Promise; `await` pauses it until a Promise settles, resuming with the value — asynchronous code written in a synchronous style. Errors surface through `try/catch`.",
    fq: "How do you run independent awaits concurrently instead of in series?",
    fa: "Start them, then await together: `await Promise.all([a(), b()])`. Writing `await a(); await b();` runs them one after the other.",
  },

  /* ============================ TypeScript ============================ */
  {
    cat: "TypeScript",
    q: "`unknown` vs `any`?",
    a: "- `any` disables type checking — anything goes (unsafe).\n- `unknown` is the **type-safe** top type: you can assign anything to it, but must **narrow** it (typeof/instanceof/guards) before use.\n\nPrefer `unknown` at boundaries (`catch`, parsed JSON).",
    fq: "What's the default type of a `catch` variable in modern TS?",
    fa: "`unknown` (with `useUnknownInCatchVariables`). You must narrow it before touching `.message`.",
  },
  {
    cat: "TypeScript",
    q: "`type` vs `interface`?",
    a: "Both describe object shapes. `interface` supports **declaration merging** and reads well for public object/class contracts. `type` is more flexible: unions, intersections, tuples, mapped/conditional types, primitive aliases.",
    fq: "Which can represent a union like `'a' | 'b'`?",
    fa: "Only `type` — `type Status = 'a' | 'b'`. An interface can't be a union.",
  },
  {
    cat: "TypeScript",
    q: "Explain generics with an example.",
    a: "Type parameters that let code work over many types while keeping their relationship:\n\n```ts\nfunction first<T>(arr: T[]): T | undefined {\n  return arr[0];\n}\nfirst([1, 2]); // T = number\n```\n\nThe return type tracks the input type.",
    fq: "How do you constrain a generic to types that have `.length`?",
    fa: "`<T extends { length: number }>`. Now only types with a numeric `length` are accepted, and `.length` is typed inside.",
  },
  {
    cat: "TypeScript",
    q: "What do `Partial`, `Pick`, `Omit`, and `Record` do?",
    a: "- `Partial<T>` — all props optional.\n- `Pick<T, K>` — keep only keys `K`.\n- `Omit<T, K>` — drop keys `K`.\n- `Record<K, V>` — object type with keys `K`, values `V`.\n\nThey derive new types instead of duplicating.",
    fq: "How would you type an object mapping user ids to `User`?",
    fa: "`Record<string, User>` (or `Record<UserId, User>` with a branded id type).",
  },
  {
    cat: "TypeScript",
    q: "What is a discriminated union?",
    a: "A union of object types sharing a **literal discriminant** field so TS can narrow:\n\n```ts\ntype Shape =\n  | { kind: 'circle'; r: number }\n  | { kind: 'square'; s: number };\n```\n\n`switch (shape.kind)` narrows each branch.",
    fq: "How do you make TS enforce every variant is handled?",
    fa: "In the `default`, assign to `const _x: never = shape`. Add a new variant and it becomes a compile error — an exhaustiveness check.",
  },
  {
    cat: "TypeScript",
    q: "`enum` vs an `as const` object?",
    a: "`enum` emits runtime code and has quirks (numeric enums are reverse-mapped, not tree-shakeable). A `const` object + `as const` is a plain, tree-shakeable value with a literal union type derived via `typeof`/`keyof`.",
    fq: "How do you derive a union from a `const` object's values?",
    fa: "```ts\nconst Roles = { admin: 'admin', user: 'user' } as const;\ntype Role = typeof Roles[keyof typeof Roles];\n```",
  },
  {
    cat: "TypeScript",
    q: "What does the `satisfies` operator do?",
    a: "It checks a value **conforms to** a type **without widening** its inferred type, so you keep precise literal types while validating the shape:\n\n```ts\nconst cfg = { port: 3000 } satisfies Config;\n```\n\n`cfg.port` stays a narrow `number`, and mismatches error.",
    fq: "How is `satisfies` different from a `: Type` annotation?",
    fa: "An annotation widens the variable to that type (losing literal precision). `satisfies` validates but preserves the narrower inferred type.",
  },
  {
    cat: "TypeScript",
    q: "What are `keyof` and `typeof` in a type context?",
    a: "- `typeof x` (in type position) gets the **type** of a runtime value.\n- `keyof T` produces a union of `T`'s **keys**.\n\nTogether, `keyof typeof obj` gives the literal union of an object's keys.",
    fq: "How do you type `get(obj, key)` so it returns the right value type?",
    fa: "```ts\nfunction get<T, K extends keyof T>(o: T, k: K): T[K] {\n  return o[k];\n}\n```\n\n`T[K]` is an indexed access type.",
  },

  /* ============================ CSS ============================ */
  {
    cat: "CSS",
    q: "How does CSS specificity work?",
    a: "Scored as (inline, ids, classes/attrs/pseudo-classes, elements): inline > `#id` (100) > `.class`/`[attr]`/`:hover` (10) > element/`::pseudo-element` (1). Higher score wins; on a tie, the **last** rule wins.",
    fq: "Where does `!important` fit, and why avoid it?",
    fa: "`!important` overrides normal specificity entirely. Avoid it because it starts wars — the only way to beat one is another `!important`, which snowballs.",
  },
  {
    cat: "CSS",
    q: "What creates a stacking context, and why does `z-index` sometimes 'not work'?",
    a: "A stacking context is a self-contained layer; `z-index` only compares **siblings within the same context**. `position`+`z-index`, `opacity < 1`, `transform`, `filter`, and `will-change` all create new contexts.",
    fq: "Why can a `z-index: 9999` element still sit behind another?",
    fa: "Because its **parent** formed a stacking context with a lower z-index. A child can't escape its parent's layer, no matter how high its own z-index.",
  },
  {
    cat: "CSS",
    q: "Flexbox vs Grid — when to use which?",
    a: "- **Flexbox**: one-dimensional (a row or a column) — toolbars, nav, distributing space along one axis.\n- **Grid**: two-dimensional (rows and columns together) — page layouts, card galleries.\n\nThey compose: grid for the macro layout, flex inside cells.",
    fq: "How do you center a box both ways with each?",
    fa: "Flex: `display:flex; justify-content:center; align-items:center`. Grid: `display:grid; place-items:center`.",
  },
  {
    cat: "CSS",
    q: "What are container queries vs media queries?",
    a: "Container queries style an element based on the size of its **containing element** (`container-type: inline-size` + `@container (min-width: ...)`), not the viewport — so components adapt to whatever slot they sit in.",
    fq: "Why are they better than media queries for component libraries?",
    fa: "A media query only knows the viewport, so a card can't tell a narrow sidebar from a wide main area. A container query lets the card respond to **its own space**.",
  },
  {
    cat: "CSS",
    q: "What do `min()`, `max()`, and `clamp()` do?",
    a: "Math functions for responsive values without media queries:\n- `min(a, b)` — the smaller.\n- `max(a, b)` — the larger.\n- `clamp(min, preferred, max)` — preferred, bounded.\n\nE.g. `font-size: clamp(1rem, 2vw + 1rem, 2rem)` for fluid type.",
    fq: "Why is `width: min(100%, 40rem)` a handy container pattern?",
    fa: "It's `40rem` on wide screens but shrinks to `100%` on small ones — a responsive max-width with no media query.",
  },
  {
    cat: "CSS",
    q: "`relative` vs `absolute` vs `fixed` vs `sticky` positioning?",
    a: "- `relative`: offset from its normal spot; keeps its space; becomes a positioning context.\n- `absolute`: removed from flow, positioned to the nearest positioned ancestor.\n- `fixed`: positioned to the viewport, stays on scroll.\n- `sticky`: relative until it hits a threshold, then sticks.",
    fq: "Why is my `sticky` element not sticking?",
    fa: "Usual causes: no threshold set (e.g. `top: 0`), an ancestor with `overflow: hidden/auto`, or the parent isn't tall enough to scroll within.",
  },

  /* ============================ HTML ============================ */
  {
    cat: "HTML",
    q: "Why associate a `<label>` with an input?",
    a: "It ties a caption to a control so screen readers announce it, and clicking the label focuses/activates the input (a bigger hit target). Use `<label for=\"id\">` or wrap the input inside the label.",
    fq: "`for`/`id` vs wrapping — any difference?",
    fa: "Both work. Explicit `for`/`id` is more robust in deeply nested markup; wrapping needs no ids but the control must sit inside the label.",
  },
  {
    cat: "HTML",
    q: "`async` vs `defer` on a `<script>`?",
    a: "- `async`: download in parallel, **execute as soon as ready** (order not guaranteed) — for independent scripts like analytics.\n- `defer`: download in parallel, **execute in order after HTML parses**, before `DOMContentLoaded` — for app scripts needing the DOM.",
    fq: "What does a plain `<script>` in `<head>` do to load time?",
    fa: "It **blocks HTML parsing** while downloading and executing, delaying render — which is why scripts go at the end of `<body>` or use `defer`.",
  },
  {
    cat: "HTML",
    q: "What does the viewport meta tag do?",
    a: "`<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">` tells mobile browsers to use the real device width instead of a fake ~980px desktop width, so responsive CSS works. Without it, mobile pages render zoomed out.",
    fq: "Why avoid `user-scalable=no`?",
    fa: "It blocks pinch-zoom — an accessibility failure for low-vision users. Let people zoom.",
  },
  {
    cat: "HTML",
    q: "What is ARIA, and what's the first rule of ARIA?",
    a: "ARIA attributes (`role`, `aria-label`, `aria-expanded`…) add semantics for assistive tech when native HTML can't express them. **First rule: don't use ARIA** if a native element (`<button>`, `<nav>`) already conveys it.",
    fq: "Why is a native `<button>` better than `<div role=\"button\">`?",
    fa: "The native button gives focusability, keyboard activation (Enter/Space), and semantics for free. The div needs `tabindex`, key handlers, and `role` — easy to get wrong.",
  },
  {
    cat: "HTML",
    q: "What does the `<iframe sandbox>` attribute do?",
    a: "It restricts embedded content. With an empty `sandbox` the iframe can't run scripts, submit forms, reach its parent, or navigate the top window. You re-grant capabilities selectively (`allow-scripts`, `allow-forms`, …).",
    fq: "Why is `sandbox=\"allow-scripts allow-same-origin\"` together risky?",
    fa: "Granting both lets the framed content run scripts **and** access its own origin, so same-origin content can strip its own sandbox attribute — defeating the isolation.",
  },

  /* ============================ Performance ============================ */
  {
    cat: "Performance",
    q: "What is the Critical Rendering Path?",
    a: "The steps to turn bytes into pixels: parse HTML→DOM, parse CSS→CSSOM, combine into the render tree, layout (geometry), then paint. Render-blocking CSS/JS stalls it and delays first paint.",
    fq: "Why is CSS render-blocking by default?",
    fa: "The browser won't paint until it has the CSSOM, to avoid a flash of unstyled content. Inline critical CSS and defer the rest to unblock paint.",
  },
  {
    cat: "Performance",
    q: "What is code splitting, and how in React?",
    a: "Breaking the bundle into chunks loaded on demand so users don't download the whole app upfront. In React: `const X = lazy(() => import('./X'))` inside a `<Suspense>` boundary; route-level splitting is the biggest win.",
    fq: "What's the risk of over-splitting?",
    fa: "Too many tiny chunks add request overhead and can cause loading waterfalls/jank. Split at meaningful boundaries (routes, heavy widgets), not every component.",
  },
  {
    cat: "Performance",
    q: "What are the Core Web Vitals?",
    a: "- **LCP** (Largest Contentful Paint) — loading; target < 2.5s.\n- **CLS** (Cumulative Layout Shift) — visual stability; < 0.1.\n- **INP** (Interaction to Next Paint) — responsiveness; < 200ms (replaced FID in 2024).",
    fq: "What commonly hurts CLS?",
    fa: "Images/ads/iframes without reserved dimensions, and web fonts causing reflow. Set width/height (or `aspect-ratio`) and tune `font-display`.",
  },
  {
    cat: "Performance",
    q: "What is tree shaking?",
    a: "Dead-code elimination that drops unused exports from the bundle, relying on **ES module** static `import`/`export` analysis. The bundler keeps only what's actually imported.",
    fq: "Why can `import * as _ from 'lodash'` defeat tree shaking?",
    fa: "Whole-namespace / CommonJS imports aren't statically analyzable per-function. Use `lodash-es` with named imports (or `lodash/get`) so only used functions ship.",
  },
  {
    cat: "Performance",
    q: "How does browser caching improve performance?",
    a: "Cached assets skip the network entirely. `Cache-Control` governs it: a long `max-age` plus **content-hashed filenames** (`app.abc123.js`) means files cache 'forever' and a new deploy just changes the hash (cache-busting).",
    fq: "Why hash filenames instead of a short max-age?",
    fa: "Hashing lets you cache immutably for a year AND update instantly — the URL changes when content changes. A short max-age forces slow revalidation every visit.",
  },
  {
    cat: "Performance",
    q: "What is lazy loading, and how do you lazy-load images?",
    a: "Deferring off-screen resources until needed. For images: `<img loading=\"lazy\">` (native) or an IntersectionObserver. It saves bandwidth and speeds the initial load.",
    fq: "Should you lazy-load the LCP/hero image?",
    fa: "No — lazy-loading the above-the-fold hero **delays** LCP. Eager-load it (even `fetchpriority=\"high\"`) and lazy-load only below-the-fold images.",
  },
  {
    cat: "Performance",
    q: "What causes memory leaks in a SPA, and how do you prevent them?",
    a: "Holding references that are never released: uncleared timers/intervals, un-removed event listeners, live subscriptions, or detached DOM kept alive in closures. Over a long session memory grows and the app janks.",
    fq: "How do you prevent leaks in a React effect?",
    fa: "Return a cleanup function that clears timers, removes listeners, aborts fetches, and unsubscribes — React runs it on unmount and before re-running the effect.",
  },

  /* ============================ Tailwind ============================ */
  {
    cat: "Tailwind",
    q: "What's the core idea — and tradeoff — of utility-first CSS?",
    a: "Compose styles from small single-purpose classes (`flex`, `pt-4`, `text-sm`) in markup instead of writing custom CSS. Pros: no naming, no dead CSS, a consistent scale, fast iteration. Con: verbose class lists mixing structure with styling.",
    fq: "How do you avoid repeating the same long class list everywhere?",
    fa: "Extract a **component** (in React) that owns the classes — the idiomatic answer — rather than `@apply`, which recreates the abstraction problems of custom CSS.",
  },
  {
    cat: "Tailwind",
    q: "How do responsive styles work in Tailwind?",
    a: "Prefixes are **min-width** breakpoints, applied mobile-first: unprefixed = all sizes, then `sm: md: lg: xl: 2xl:` apply from that width up. `text-sm md:text-base` = small by default, larger from `md`.",
    fq: "Does `md:flex` mean 'only on medium screens'?",
    fa: "No — it means 'from `md` **and up**' (min-width), so it also applies at lg and xl. Use `max-*` variants for a range that's only medium.",
  },
  {
    cat: "Tailwind",
    q: "How does dark mode work in Tailwind?",
    a: "Prefix utilities with `dark:` (e.g. `bg-white dark:bg-gray-900`). It activates via OS preference (`media` strategy) or a `.dark` class on a parent (`class`/`selector` strategy) that you toggle in JS.",
    fq: "Why pick the `class` strategy over `media`?",
    fa: "It lets users choose a theme manually (and persist it), independent of the OS setting — you toggle a `dark` class on `<html>`.",
  },
  {
    cat: "Tailwind",
    q: "What does `@apply` do, and when should you avoid it?",
    a: "`@apply` inlines utility classes into a custom CSS rule (`.btn { @apply px-4 py-2 rounded; }`). Use it sparingly — small primitives, third-party overrides. Overusing it rebuilds the semantic-CSS problems Tailwind set out to avoid.",
    fq: "What's the preferred alternative in a React app?",
    fa: "A component that encapsulates the utilities (`<Button>`), so the abstraction lives in one place and stays composable.",
  },
  {
    cat: "Tailwind",
    q: "How does Tailwind keep the CSS bundle small?",
    a: "It scans your source files (the `content` globs) and generates **only the classes you actually use**. Unused utilities never reach production CSS, so the bundle stays tiny no matter how many utilities exist.",
    fq: "Why can dynamic class names like `` `text-${color}-500` `` break in production?",
    fa: "The scanner reads class strings **statically** — it can't evaluate template literals, so those classes get purged. Use complete literal class names or a safelist.",
  },
];
